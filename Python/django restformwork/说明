Django DRF 简单说明
    1、APIView # 只是封装了django 的views方法
    2、序列化  serializers
    3、认证
    4、权限
    5、频现
    记不全了 代用到时在补入


接口项目： 接口项目不会写入这个文档中，但写完项目后 会将项目放入这层目录中，想使用时 把目录复制出去执行即可。
项目要求：
    1、如果是vue+django/flask时 将存入已生成后的前端，即不用在进行 npm build 操作
    2、后台执行 包括 创建数据库等初始化操作 写入一个shell脚本中执行。执行后必须确保服务已经启动起来。可使用即可